// API Configuration
const API_BASE_URL = 'http://localhost:5000';
const API_ENDPOINTS = {
    books: `${API_BASE_URL}/api/books`,
    borrow: `${API_BASE_URL}/api/borrow`
};

// User roles
const USER_ROLES = {
    ADMIN: 'admin',
    USER: 'user'
};
